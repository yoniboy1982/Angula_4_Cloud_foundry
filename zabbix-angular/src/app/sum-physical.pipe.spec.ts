import { SumPhysicalPipe } from './sum-physical.pipe';

describe('SumPhysicalPipe', () => {
  it('create an instance', () => {
    const pipe = new SumPhysicalPipe();
    expect(pipe).toBeTruthy();
  });
});
